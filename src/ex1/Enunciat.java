package ex1;

public class Enunciat {
    /*

        Dins la classe "Main" trobaràs un codi que fa el següent:
            1. Llegeix un string de l'usuari
            2. Ho passa a un array de chars i, després, a un array d'enters (on cada enter és l'equivalent de la taula
               ASCII extended (https://theasciicode.com.ar/).
            3. Encripta d'una forma molt senzilla: suma +1 a cada codi ASCII (a -> b; P -> Q; # -> $) però l'últim
               codi (255) passa a ser 0 (overflow). 
            4. Torna a transormar cada número enter ASCII en un char per després convertir l'array de chars en un
               string.
            5. Mostra per pantalla el missatge encriptat.
            6. Per a provar que funciona a la inversa, repeteix els passsos anteriors a la inversa (resta 1 al codi
               ASCII)
            7. Ho mostra per pantalla.


        # Aplica el mètode de refacció "Extracció de mètode" sobre els passos 2, 3, 4 i 6. Cadascun d'aquests passos
          o "steps" serà un mètode nou. S'espera que n'obtinguis EXACTAMENT 4 mètodes nous.

        # Explica quins canvis has hagut de fer al codi perquè tot funcioni correctament.

     */
}

